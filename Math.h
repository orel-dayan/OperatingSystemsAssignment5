#pragma once
#include <math.h>

/**
 * @brief Check if a number is prime for generating numbers.
 *
 * @param n Number to be checked.
 * @return int 1 if the number is prime, 0 otherwise.
 */

int isPrime(unsigned int n);


